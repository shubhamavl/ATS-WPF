<Window x:Class="ATS_WPF.Views.StatusHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:ATS_WPF.ViewModels"
        mc:Ignorable="d"
        Title="Status History" Height="450" Width="800"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="System Status History" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding StatusEntries}" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat='HH:mm:ss.fff'}" Width="100"/>
                <DataGridTextColumn Header="Status" Binding="{Binding SystemStatus}" Width="60"/>
                <DataGridTextColumn Header="Errors" Binding="{Binding ErrorFlags, StringFormat='0x{0:X2}'}" Width="60"/>
                <DataGridTextColumn Header="Relay" Binding="{Binding RelayState}" Width="80"/>
                <DataGridTextColumn Header="Firmware" Binding="{Binding FirmwareVersion}" Width="80"/>
                <DataGridTextColumn Header="CAN Hz" Binding="{Binding CanTxHz}" Width="70"/>
                <DataGridTextColumn Header="ADC Hz" Binding="{Binding AdcSampleHz}" Width="70"/>
                <DataGridTextColumn Header="Uptime" Binding="{Binding UptimeSeconds}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <Button Grid.Row="2" Content="Clear History" Command="{Binding ClearHistoryCommand}" Width="100" HorizontalAlignment="Right" Margin="0,10,0,0"/>
    </Grid>
</Window>

