<UserControl x:Class="ATS_WPF.Views.Controls.ConnectionConfigurationPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ATS_WPF.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
    </UserControl.Resources>
    
    <GroupBox Header="CAN Adapter Configuration">
        <StackPanel Margin="10">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Adapter Type:" VerticalAlignment="Center" Width="120" FontWeight="SemiBold"/>
                <ComboBox ItemsSource="{Binding AdapterTypes}" SelectedItem="{Binding SelectedAdapterType}"
                          Width="180" Margin="0,0,10,0"/>
                <CheckBox Content="Shared Bus (Single Port)" IsChecked="{Binding UseSharedBusForHmv}" 
                          VerticalAlignment="Center" 
                          Visibility="{Binding IsHmvMode, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
            
            <!-- USB Settings (Single Port - Two-Wheeler / LMV) -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsUsbAdapter}" Value="True"/>
                                    <Condition Binding="{Binding IsSinglePortMode}" Value="True"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible"/>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <TextBlock Text="COM Port:" VerticalAlignment="Center" Width="120" FontWeight="SemiBold"/>
                <ComboBox ItemsSource="{Binding AvailablePorts}" SelectedItem="{Binding SelectedPort}"
                          Width="150" Margin="0,0,10,0"/>
                <Button Content="🔄" Command="{Binding RefreshPortsCommand}" Width="25" Height="25" 
                        ToolTip="Refresh Ports"/>
            </StackPanel>

            <!-- USB Settings (Dual Port - HMV) -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsUsbAdapter}" Value="True"/>
                                    <Condition Binding="{Binding IsHmvMode}" Value="True"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible"/>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <TextBlock Text="Left COM Port:" VerticalAlignment="Center" Width="120" FontWeight="SemiBold"/>
                        <ComboBox ItemsSource="{Binding AvailablePorts}" SelectedItem="{Binding SelectedLeftPort}"
                                  Width="150" Margin="0,0,10,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Right COM Port:" VerticalAlignment="Center" Width="120" FontWeight="SemiBold"/>
                        <ComboBox ItemsSource="{Binding AvailablePorts}" SelectedItem="{Binding SelectedRightPort}"
                                  Width="150" Margin="0,0,10,0"/>
                    </StackPanel>
                </StackPanel>
                
                <Button Content="🔄" Command="{Binding RefreshPortsCommand}" Width="25" Height="55" 
                        ToolTip="Refresh Ports" VerticalAlignment="Center"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Baud Rate:" VerticalAlignment="Center" Width="120" FontWeight="SemiBold"/>
                <ComboBox ItemsSource="{Binding BaudRates}" SelectedItem="{Binding SelectedBaudRate}"
                          Width="120"/>
            </StackPanel>
            
            <TextBlock Text="Select CAN adapter type. USB-CAN-A Serial uses COM port." 
                       FontSize="10" Foreground="#FF95A5A6" Margin="0,0,0,10"/>
        </StackPanel>
    </GroupBox>
</UserControl>

